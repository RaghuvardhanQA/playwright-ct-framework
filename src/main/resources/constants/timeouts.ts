// Timeout constants used across component tests.
// Keep all duration literals here so they can be tuned in one place.

export const SMALL_TIMEOUT = 2000;
export const DEFAULT_TIMEOUT = 5000;
export const LARGE_TIMEOUT = 10000;
